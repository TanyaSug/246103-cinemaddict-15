// export const countUserDetails = (state, element) => {
//   state.watchlistCount += element.userDetails.watchlist ? 1 : 0;
//   state.historyCount += element.userDetails.alreadyWatched ? 1 : 0;
//   state.favoritesCount += element.userDetails.favorite ? 1 : 0;
//   return state;
// };

